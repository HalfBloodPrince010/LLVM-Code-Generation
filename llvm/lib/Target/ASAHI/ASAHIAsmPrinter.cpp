//===-- ASAHIAsmPrinter.cpp - ASAHI LLVM assembly writer ------------------===//
//
// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
// See https://llvm.org/LICENSE.txt for license information.
// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
//
//===----------------------------------------------------------------------===//
//
// This file contains a printer that converts from our internal representation
// of machine-dependent LLVM code to the ASAHI assembly language.
//
//===----------------------------------------------------------------------===//

#include "ASAHI.h"
#include "ASAHIInstrInfo.h"
#include "ASAHITargetMachine.h"
#include "MCTargetDesc/ASAHIInstPrinter.h"
#include "TargetInfo/ASAHITargetInfo.h"
#include "llvm/CodeGen/AsmPrinter.h"
#include "llvm/CodeGen/MachineConstantPool.h"
#include "llvm/CodeGen/MachineFunctionPass.h"
#include "llvm/CodeGen/MachineInstr.h"
#include "llvm/MC/MCInst.h"
#include "llvm/MC/MCStreamer.h"
#include "llvm/MC/MCSymbol.h"
#include "llvm/MC/TargetRegistry.h"
#include "llvm/Support/raw_ostream.h"
using namespace llvm;

#define DEBUG_TYPE "asm-printer"

namespace {

class ASAHIAsmPrinter: public AsmPrinter {
public:
    explicit ASAHIAsmPrinter(TargetMachine &TM,
                             std::unique_ptr<MCStreamer> Streamer)
            : AsmPrinter(TM, std::move(Streamer)) {}
    
    StringRef getPassName() const override { return "ASAHI Assembly Printer"; }

    void emitInstruction(const MachineInstr *MI) override;
};
} // end anonymous namespace

/// Returns true if MO translates to an MCOperand.
/// When false is returned, this means that no MCOperand needs to
/// be produced for this MO (e.g., for an implicit MO.)
static bool lowerOperand(const MachineOperand &MO, MCOperand &MCO) {
  switch (MO.getType()) {
  default:
    llvm_unreachable("unknown operand type");
  case MachineOperand::MO_Register:
    // Ignore all implicit register operands.
    if (MO.isImplicit())
      return false;
    MCO = MCOperand::createReg(MO.getReg());
    break;
  case MachineOperand::MO_RegisterMask:
    // Regmasks are like implicit defs.
    return false;
  case MachineOperand::MO_Immediate:
    MCO = MCOperand::createImm(MO.getImm());
    break;
  case MachineOperand::MO_MachineBasicBlock:
  case MachineOperand::MO_GlobalAddress:
  case MachineOperand::MO_ExternalSymbol:
  case MachineOperand::MO_MCSymbol:
  case MachineOperand::MO_JumpTableIndex:
  case MachineOperand::MO_ConstantPoolIndex:
  case MachineOperand::MO_BlockAddress:
    llvm_unreachable("not yet implemented");
  }
  return true;
}

/// Translate a MachineInstr to a MCInst.
static MCInst MachineInstrToMCInst(const MachineInstr &MI) {
  MCInst TmpInst;
  TmpInst.setOpcode(MI.getOpcode());

  for (const MachineOperand &MO : MI.operands()) {
    MCOperand MCOp;
    if (lowerOperand(MO, MCOp))
      TmpInst.addOperand(MCOp);
  }
  return TmpInst;
}


void ASAHIAsmPrinter::emitInstruction(const MachineInstr *MI) {
  // This function is autogenerated as part of the InstrInfo TableGen backend.
  /*
  Suppose your ISA has optional extensions/features.

    def FeatureVector : SubtargetFeature<
      "vector",
      "HasVector",
      "true",
      "Enable vector instructions"
    >;

  and an instruction:
    
    def VADD : ASAHIInstruction<
      "vadd", "$dst, $src1, $src2" > {
           let Predicates = [HasVector];
    }
    
    Now if the featureBits are not set, lets say HasVector is not been
    set, then printing VADD is not right.
    
    This function verifies such predicate
  */
  ASAHI_MC::verifyInstructionPredicates(MI->getOpcode(),
                                        getSubtargetInfo().getFeatureBits());

  MCInst TmpInst = MachineInstrToMCInst(*MI);
  EmitToStreamer(*OutStreamer, TmpInst);
}


// Register the AsmPrinter in the ASAHI target for other tools to find
// it (like Clang.)
extern "C" LLVM_EXTERNAL_VISIBILITY void LLVMInitializeASAHIAsmPrinter() {
  RegisterAsmPrinter<ASAHIAsmPrinter> Tmp(getTheAsahiTarget());
}
